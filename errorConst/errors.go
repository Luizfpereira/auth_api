package errorConst

const (
	EMAIL_REGISTERED = "email already registered"
	PASSWORD_INVALID = `password should at least contain a number, a special character and an upper and lower case. The length should not be less than 7 characters.`
)
